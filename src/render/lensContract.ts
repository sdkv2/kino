/** Shared author contract for backdrop-sampling lens effects (fs-free — page + node). */
export const DEFAULT_LENS_ID = "liquid-glass";
export const LENS_CLASS_RE = /\bkino-lens\b/;
export const LENS_SELECTOR = ".kino-lens";
/** SVG silhouette child on a lens element (SMIL / path morph). */
export const LENS_SHAPE_CLASS = "kino-lens-shape";
